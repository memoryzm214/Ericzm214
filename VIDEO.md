# 怎么把视频放进课件

课件会**按下面的顺序**自动找视频，找到哪个用哪个：

| 优先级 | 方式 | 适合场景 |
| --- | --- | --- |
| 1 | 同目录 `assets/hook.mp4` | 平时改课件、带文件夹拷贝 |
| 2 | 页面上点「本机文件」选的视频 | 临时换片、借别人电脑上课 |
| 3 | B 站在线播放器 | 用 `http://` 打开时 |

---

## 方式一：把视频放进 assets 文件夹（推荐）

把视频命名为 **`hook.mp4`**，放进课件旁边的 `assets/` 文件夹：

```
你的文件夹/
├── slides.html
└── assets/
    ├── hook.mp4      ← 放这里
    └── ...其他图片
```

双击 `slides.html`，视频自动出现并可播放。**不需要联网。**

> 用 `slides-single.html`（图片已内嵌）也一样：在它旁边建一个 `assets/` 文件夹放 `hook.mp4` 即可。

---

## 方式二：把视频烧进 HTML，得到真正的单文件

适合发给别人、或用 U 盘拷到教室电脑——**只有一个文件，不会丢东西**。

```bash
python3 tools/embed-video.py 你的视频.mp4
```

默认读 `slides-single.html`，生成 `slides-with-video.html`。也可以指定：

```bash
python3 tools/embed-video.py 你的视频.mp4 -i slides-single.html -o 上课用.html
```

生成的文件双击即可，视频内嵌其中，不需要任何附带文件夹，也不需要联网。

**注意体积**：内嵌后文件大小约为视频的 1.35 倍。建议把视频压到 **30MB 以内**（1 分钟 720p 通常足够），否则打开会变慢。

---

## 方式三：上课现场临时选

打开课件 → 第 2 页 → 点播放器下方的 **「本机文件」** → 选一个视频。

选过一次会被浏览器记住（存在 IndexedDB 里），下次打开还在，不用重选。

---

## 视频格式建议

浏览器最通用的组合是 **MP4 容器 + H.264 视频 + AAC 音频**。如果你的视频打不开，用 ffmpeg 转一下：

```bash
ffmpeg -i 原视频.mp4 -c:v libx264 -profile:v high -pix_fmt yuv420p \
       -c:a aac -b:a 128k -movflags +faststart 转好的.mp4
```

想同时压小体积，加上 `-crf 28 -vf scale=-2:720`。

---

## 关于在线播放

课件里预置了三个 B 站视频源（沙海重逢 / 5000 美元 5 万棵树 / 完整始末）。

**但是**：B 站、YouTube 这类平台都拒绝被 `file://` 页面内嵌。所以双击打开时在线播放一定不可用，这不是课件的问题。

如果确实想用在线源，在课件所在目录执行：

```bash
python3 -m http.server
```

然后浏览器打开 `http://localhost:8000/slides.html`，在线播放器就能正常工作了。

**上课还是建议用方式一或方式二**——不依赖网络，最稳。
